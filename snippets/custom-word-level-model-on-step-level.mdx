<CodeGroup>
```python Python
    agent.click("{click-word}", model=ModelComposition([ModelDefinition(
                task="e2e_ocr",
                architecture="easy_ocr",
                version="1",
                interface="online_learning",
                useCase="{workspace-id}",
                tags=["{tag}"]
            )]))
    ```
    ```typescript TypeScript
    await aui.click().text("{click-word}").exec([{
        "task": "e2e_ocr",
        "architecture": "easy_ocr",
        "version": "1",
        "interface": "online_learning",
        "useCase": "{workspace-id}",
        "tags": ["{tag}"]
    }])
    ```        
</CodeGroup>